import styled from 'styled-components'
import utilStyles from '../../utils.css'

const H1 = styled.h1`
	${utilStyles.headingXl}
`

const DateContainer = styled.div`
	${utilStyles.lightText}
`

export default { H1, DateContainer }
